apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - configmap.yaml
  - secret.yaml
  - deployment.yaml

# The matrix namespace is created by the continuwuity app, which this depends
# on anyway (the shared TURN secret is only useful if the homeserver hands it
# out). It is deliberately not re-declared here.
