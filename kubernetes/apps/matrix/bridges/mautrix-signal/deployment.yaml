apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mautrix-signal
  namespace: matrix
spec:
  accessModes: [ReadWriteOnce]
  # SQLite and the olm crypto store need real POSIX locking and honest fsync,
  # which the NFS class does not provide. See the storage notes in AGENT.md.
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
---
apiVersion: v1
kind: Service
metadata:
  name: mautrix-signal
  namespace: matrix
spec:
  selector:
    app.kubernetes.io/name: mautrix-signal
  ports:
    - name: appservice
      port: 29328
      targetPort: appservice
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mautrix-signal
  namespace: matrix
spec:
  replicas: 1
  # SQLite is single-writer; a RollingUpdate would briefly run two pods against
  # one database file on every image bump.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: mautrix-signal
  template:
    metadata:
      annotations:
        # Mounted Secrets do not roll a Deployment on their own and this
        # cluster has no Reloader, so the config hash rides in the pod
        # spec: change the config, change this, get a new pod.
        checksum/config: 84dc69376a34034e
      labels:
        app.kubernetes.io/name: mautrix-signal
    spec:
      securityContext:
        fsGroup: 1337
      containers:
        - name: bridge
          image: dock.mau.dev/mautrix/signal:v26.09
          # -n (--no-update) stops the bridge rewriting its own config on
          # startup: the file is a read-only Secret mount and the write is fatal.
          command: [/usr/bin/mautrix-signal, -c, /config/config.yaml, -n]
          ports:
            - name: appservice
              containerPort: 29328
          volumeMounts:
            - name: config
              mountPath: /config/config.yaml
              subPath: config.yaml
              readOnly: true
            - name: data
              mountPath: /data
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              memory: 512Mi
      volumes:
        - name: config
          secret:
            secretName: mautrix-signal
        - name: data
          persistentVolumeClaim:
            claimName: mautrix-signal
