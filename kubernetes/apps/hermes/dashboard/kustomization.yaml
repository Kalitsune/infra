apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # The `hermes` namespace and its Secrets are owned by the `hermes`
  # Kustomization, which this one dependsOn — nothing namespace-scoped is
  # created here beyond the dashboard's own three objects.
  - oidc-secret.yaml
  - configmap.yaml
  - httproute.yaml
