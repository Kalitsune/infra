apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: radar
  namespace: radar
spec:
  parentRefs:
    - name: apps
      namespace: envoy-gateway-system
  hostnames:
    - radar.kalitsune.net
  rules:
    - backendRefs:
        - name: radar
          port: 9280
