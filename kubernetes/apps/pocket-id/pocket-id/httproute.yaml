apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: pocket-id
  namespace: pocket-id
spec:
  parentRefs:
    - name: apps
      namespace: envoy-gateway-system
  hostnames:
    - id.kalitsune.net
  rules:
    - backendRefs:
        - name: pocket-id
          port: 80
